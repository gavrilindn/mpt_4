import { getTimeBeforeNY } from "../lib";
import { expect } from 'chai';
import { assert } from "console";

describe('time to NY', () => { // the tests container
    it('time from 1 Jan', () => { // the single test
        expect(false).to.be.false;
    });
});
