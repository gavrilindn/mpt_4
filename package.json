{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/jest": "^29.0.3",
    "@types/mocha": "^9.1.1",
    "lerna": "^5.5.2",
    "mocha": "^10.0.0",
    "prettier": "^2.7.1"
  },
  "scripts": {
    "test": "lerna run test",
    "prettier": "prettier --check packages",
    "prettier:write": "prettier --write packages"
  }
}
